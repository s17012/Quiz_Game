<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.ac.it_college.std.s17012.quizgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AnswerActivity" />
        <activity android:name=".Answer2Activity" />
        <activity android:name=".Answer3Activity" />
        <activity android:name=".ZyanruActivity" />
        <activity android:name=".ZyanrusentakuActivity" />
        <activity android:name=".Zyanru2Activity" />
        <activity android:name=".Zyanrusentaku2Activity"></activity>
    </application>

</manifest>